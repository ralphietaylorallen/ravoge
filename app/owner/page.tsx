import { SetupPending } from "@/components/setup-pending";

export default function OwnerPage() {
  return <SetupPending audience="Owner" eyebrow="Owner portal" description="The owner workspace will bring gym operations, coach access, client oversight, and business visibility together in one focused view." />;
}
