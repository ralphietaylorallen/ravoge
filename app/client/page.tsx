import { SetupPending } from "@/components/setup-pending";

export default function ClientPage() {
  return <SetupPending audience="Client" eyebrow="Client portal" description="Clients will have a private view of their own program, progress, schedule, and training history—nothing more and nothing less." />;
}
