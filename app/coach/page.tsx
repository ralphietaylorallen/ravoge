import { SetupPending } from "@/components/setup-pending";

export default function CoachPage() {
  return <SetupPending audience="Coach" eyebrow="Coach portal" description="Coaches will sign in individually on shared gym iPads to pick up the right session context and keep every client moving forward." />;
}
